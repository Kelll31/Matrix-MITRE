# 🔴 MITRE ATT&CK Matrix Viewer

Полнофункциональное веб-приложение для просмотра, управления и анализа матрицы MITRE ATT&CK с автоматическим обновлением.

## 🚀 Особенности

✅ **FastAPI Backend** - асинхронный API на Python  
✅ **Vue.js 3 Frontend** - современный интерактивный интерфейс  
✅ **Автоматическое обновление** - настраиваемый интервал обновления (1 час - 7 дней)  
✅ **Кэширование** - локальное кэширование для быстрой загрузки  
✅ **Поиск и фильтрация** - встроенный поиск техник  
✅ **Красивый UI** - Bootstrap + Element UI + Naive UI  
✅ **Полная поддержка кириллицы** - все на русском языке  

## 🛠️ Технологический стек

### Backend
- **FastAPI** - веб-фреймворк
- **aiohttp** - асинхронные HTTP запросы
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **Vue.js 3** - JavaScript фреймворк
- **TypeScript** - типизированный JavaScript
- **Axios** - HTTP клиент
- **Lodash** - утилиты для работы с данными
- **jQuery** - DOM манипуляция
- **Select2** - улучшенные select элементы
- **Bootstrap 5** - CSS фреймворк
- **Element UI** - UI компоненты
- **Naive UI** - дополнительные компоненты
- **core-js** - полифиллы

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/Kelll31/Matrix-MITRE.git
cd Matrix-MITRE
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск приложения

```bash
python main.py
```

Приложение будет доступно по адресу: **http://localhost:8000**

## 📖 API Endpoints

### Получение данных

```bash
# Полная матрица
GET /api/matrix

# Статистика
GET /api/statistics

# Тактики
GET /api/matrix/tactics

# Техники конкретной тактики
GET /api/matrix/tactic/{tactic_name}

# Поиск техник
GET /api/search?q=query
```

### Управление

```bash
# Изменить интервал обновления
POST /api/settings/update-interval
Content-Type: application/json
{"interval": "24_hours"}

# Принудительное обновление матрицы
POST /api/matrix/refresh
```

## ⚙️ Доступные интервалы обновления

- `1_hour` - 1 час
- `6_hours` - 6 часов
- `12_hours` - 12 часов
- `24_hours` - 24 часа (по умолчанию)
- `7_days` - 7 дней

## 🔄 Автоматические обновления

Приложение автоматически загружает новую версию матрицы MITRE согласно установленному интервалу:

1. При запуске - загружает из GitHub или из кэша
2. В фоне - запускается асинхронная задача обновления
3. Кэширование - сохраняет данные локально для быстрой загрузки
4. Метаданные - отслеживает время последнего обновления

## ✨ Используемые технологии

- 🔴 **Vue.js 3** - прогрессивный JavaScript фреймворк
- 🐍 **FastAPI** - быстрый веб-фреймворк
- 📦 **Axios** - HTTP клиент
- 🔧 **Lodash** - утилиты для работы с данными
- 🎨 **Bootstrap 5** - CSS фреймворк
- 🎭 **Element UI** - компоненты
- 💎 **Naive UI** - дизайн-система
- ⚙️ **Select2** - расширенные селекты
- 📚 **jQuery** - DOM манипуляция

---

**Сделано с ❤️ для пентестеров и специалистов по безопасности**